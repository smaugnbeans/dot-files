[user]
	email = jean@vannoppen.co
	name = Jean VanNoppen
	user = jean
[push]
	default = matching
[color]
	ui = auto
[color "branch"]
	current = yellow bold
	local = green bold
	remote = cyan bold
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
	whitespace = red reverse
[color "status"]
	added = green bold
	changed = yellow bold
	untracked = red bold
[alias]
	l = log --oneline
	lg = log --oneline --graph --decorate
	lp = log —-pretty=oneline
	#############
	pshm = push -u heroku master
